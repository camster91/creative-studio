[project]
name = "creative-studio"
version = "4.5.0"
description = "Iterative AI image generation workflow for Pi"
requires-python = ">=3.10"
dependencies = [
    "google-genai>=2.0",
    "Pillow>=11.0",
    "flask>=3.1.3",
]
